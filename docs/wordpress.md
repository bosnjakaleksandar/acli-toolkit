# WordPress

WordPress projects support Docker Compose or Lando, configurable MySQL and WordPress versions, starter themes, custom theme repositories, branch selection, and optional plugin setup scripts.

Use:

```bash
create-project --preset wordpress
create-project --preset wordpress-woo
```
