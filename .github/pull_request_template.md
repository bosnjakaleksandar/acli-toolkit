## What does this change and why?

## Test plan

- [ ] `npm test` passes
- [ ] Added/updated tests for the behavior change
- [ ] If this touches process spawning, shell command construction, or `.acli/config.yaml` parsing: added a test proving the specific injection/trust scenario is handled

## Security-relevant?

If this change touches command execution, secret handling, or generated templates (Docker/Lando/gitignore), briefly note the security implication here (or "none").
